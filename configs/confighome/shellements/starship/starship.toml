# Disable the blank line at the start of the prompt
format = '$time$all'
add_newline = false

[directory]
truncation_length = 10
truncate_to_repo = false

[time]
disabled = false
format = "🕙 [\\[ $time \\]]($style) \n"
time_format = '%T'
